#include <stdio.h>
/**
* main - sprint alphabet in lowercase
* Return: Always 0
*/
int main(void)
{
	char x;

	if (x = e && x = q)
		return (0);
	for (x = 'a'; x <= 'z'; x++)
		putchar(x);

	putchar('\n');
	return (0);
}
