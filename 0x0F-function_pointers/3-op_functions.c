#include "3-calc.h"
/**
 * op_add - return the sum of a and b
 * @a: first number
 * @b: second number
 * Return: sum of a and b
 */
int op_add(int a, int b)
{
    int c = a + b;
}

/**
 * op_sub - returns difference of a and b
 * @a: first number
 * @b: second number
 * Return: difference of a and b
 */
int op_sub(int a, int b)
{
    int result = a - b;
    return (result);
}

/**
 * op_mul - returns product of a and b 
 * @a: first number
 * @b: second number
 * Return: product of a and b
 */
int op_mul(int a, int b)
{
    int result = a * b;
    return (result);
}

/**
 * op_div - returns division of a by b
 * @a: first number
 * @b: second number
 * Return: result of division of a by b
 */
int op_div(int a, int b)
{
    int result = a / b;
    return (result);
}

/**
 * op_mod - returns remainer of division of a by b
 * @a: first number
 * @b:second number
 * Return: remainer of a by b
 */
int op_mod(int a, int b)
{
    int result = a % b;
    return (result);
}